node_modules/
data/
logs/
.env
.env.local
.env.*.local
*.db
*.db-journal
.DS_Store
.claude/settings.local.json

# Tenant-specific data — belongs to a separate private repo, not this one.
/tenants/
/pi-config/

# Local prototypes / mockups — may contain real PII for design work. Keep out of public repo.
/prototypes/
